[package]
name = "polkavm-ui-wire"
version = "0.2.0"
edition = "2021"
description = "Versioned host-neutral UI output wire protocol for PolkaVM applications"
license = "MPL-2.0"
repository = "https://github.com/paritytech/polkavm-host-runtime"
publish = false

[lib]
crate-type = ["rlib"]
